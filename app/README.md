# AirToss app

Flutter UI for Android, iOS, Linux, macOS, and Windows. Protocol and routing decisions belong to the Rust core in `../crates/core`.

```sh
flutter analyze
flutter test
```

The current Ubuntu development host also needs `clang` and the GTK 3 development headers before the Linux runner can be built.
